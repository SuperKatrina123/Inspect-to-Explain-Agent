# Vite proxy 转发目标（后端 server 地址）
# 本地开发默认不需要设置，自动使用 http://localhost:3001
#
# 远端 server 场景（复制为 .env.local 并取消注释）：
# VITE_PROXY_TARGET=https://your-remote-server.com
# VITE_PROXY_TARGET=http://10.0.1.42:3001
#
# .env.local 已在 .gitignore 中，不会提交到仓库
